type: group
fields:
  upstream_seo_title:
    label: SEO Title
    type: text
    icon: font
    placeholder: "{{ page.title }}"
    width: 1/2
    required: false
  upstream_seo_description:
    label: Meta Description
    type: textarea
    size: small
    placeholder: "{{ page.abstract }}"
