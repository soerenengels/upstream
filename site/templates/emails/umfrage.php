Eine neue Antwort auf die Umfrage "<?= $poll_title ?>"<> wurde eingereicht.

Folgende Felder wurden eingetragen:

<?= var_dump($data["frage"]) ?>

<?php /* foreach ($data as $field => $value): ?>
<?= mb_strtoupper($field) ?>:
====================================================
<?= $value ?>


<?php endforeach; */ ?>